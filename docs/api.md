# API

Base `/api/v1/`. Authentication, customers, products, quotes, invoices, payments, sales, inventory, expenses, reports and AI endpoints are defined in the master specification. All business endpoints require authenticated context and server-side authorization.
