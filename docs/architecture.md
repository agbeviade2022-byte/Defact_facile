# Architecture

Flutter → NestJS REST → Supabase PostgreSQL/Auth/Storage. AI Gateway, PDF, Resend, WhatsApp and FNE adapters remain behind backend services. Multi-tenant isolation is enforced server-side with memberships, permissions and RLS.
