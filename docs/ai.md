# AI

AI Gateway with provider abstraction. Use structured outputs and tool calling. Financial calculations remain deterministic. Mutating operations require explicit user confirmation before execution.
