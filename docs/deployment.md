# Deployment

Maintain development, staging and production environments. Keep secrets outside Git. CI should run lint, unit tests, integration tests, builds and security checks before production deployment.
