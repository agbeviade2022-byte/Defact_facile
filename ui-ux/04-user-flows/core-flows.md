# Core flows
Signup → email verification → profile → workspace. Quote → accept → invoice. Invoice → payment. Purchase → reception → stock. Sale → payment → stock out. AI → preview → confirmation → backend execution.
