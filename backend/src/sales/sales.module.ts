import { Module } from '@nestjs/common';

/**
 * sales — implemented in a later mission. Kept as an explicit module so the
 * application graph mirrors docs/architecture.md from day one.
 */
@Module({})
export class SalesModule {}
